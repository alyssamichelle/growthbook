---
draft: true
title: Install and Connect the GrowthBook Visual Editor
description: Install the GrowthBook Visual Editor Chrome extension and connect it to your GrowthBook account with a single click. No copying API tokens by hand.
sidebar_label: Install and connect
slug: /app/visual/install-and-connect
---

import MaxWidthImage from '@site/src/components/MaxWidthImage';
import CommercialFeature from '@site/src/components/CommercialFeature';

<CommercialFeature feature="visual-editor" />

The GrowthBook Visual Editor is a standalone Chrome extension built for **non-technical teammates** who want to create website variants and run experiments without filing an engineering ticket. Setup takes about thirty seconds: install the extension, then click **Connect with GrowthBook**.

:::info Early access

The AI Visual Editor is free to try on **GrowthBook Cloud** during early access. Connect the extension to your Cloud account to get started.

:::

We took UX inspiration from tools like Webflow (clear creation flow) while avoiding the clutter of legacy A/B tools. The side panel stays out of your page content so you can see the real site while you edit.

## 1. Install the extension

Install the **GrowthBook Visual Editor** extension from the Chrome Web Store:

- **Chrome, Edge, Brave, Arc, and most Chromium browsers** — [GrowthBook Visual Editor on the Chrome Web Store](https://chromewebstore.google.com/detail/growthbook-visual-editor/nbomejknbpkcpjdagefhichaajpoempk)

:::note Listing availability

The Chrome Web Store listing may not be publicly available until early access opens. If the link does not load yet, your GrowthBook early-access email will include install instructions when the extension is live.

:::

Firefox support is not available yet. The legacy [GrowthBook DevTools extension](/tools/chrome-extension) still supports Firefox for feature-flag debugging, but not this side-panel Visual Editor.

## 2. Open the side panel

<MaxWidthImage maxWidth={300}>
  ![The GrowthBook Visual Editor Getting Started](/images/visual-editor/ve-connect-step1.png)
</MaxWidthImage>
Open the extension menu from the top right, and choose the GrowthBook Visual Editor Extension.

[alyssa insert a screenshot of exactly this feature here: the browser toolbar with the extension icon and "GrowthBook Visual Editor" selected in the extensions menu.]

The first time you open the side panel, you'll see a **Connect with GrowthBook** button.

## 3. Click "Connect with GrowthBook"

<MaxWidthImage maxWidth={300}>
  ![The GrowthBook Visual Editor Getting Started](/images/visual-editor/ve-connect-step2.png)
</MaxWidthImage>

Click the `connect` button. A new tab opens to GrowthBook. If you're already signed in, the page silently generates a Visual Editor
access key tied to your account and hands it back to the extension via a secure browser-side message. The tab closes itself,
the side panel updates to show you're connected, and you're done.

If you're not already signed in, you'll see the GrowthBook login page first. If you are only part of one organization,
the key generation will happen automatically. If you part of multiple organizations, you'll see a dropdown to choose
which one to connect to. After you choose, the key is generated and passed to the extension.

That's it. No copying tokens, no pasting strings, no remembering where the personal-access-token page lives.

[alyssa record a video for this section: full connect flow from empty side panel through "You're connected," including multi-org picker if easy to demo.]

<MaxWidthImage maxWidth={300}>
  ![The GrowthBook Visual Editor Getting Started](/images/visual-editor/ve-connect-step3.png)
</MaxWidthImage>

### Where the key is stored

The access key is stored in the extension's local storage on your machine. It's not synced across browsers, and it's only sent in the `Authorization` header of requests to the GrowthBook API host the extension is configured for.

### Self-hosted GrowthBook

The one-click flow works on GrowthBook Cloud and any self-hosted instance the extension has previously synced with. If you're on a fresh extension install pointing at a self-hosted instance, you may need to:

1. Open the gear menu → **API host** and set your host (e.g., `https://api.growthbook.io` or `https://localhost:3100`).
2. Paste in your personal access token from your GrowthBook. You can generate a PAT from https://app.growthbook.io/account/personal-access-tokens or the equalivent page on your self-hosted instance.
3. Click **Connect with GrowthBook**.

## 4. The gear menu

Once connected, there's a gear icon in the side panel header. Open it for:

- **Status** — shows whether the extension is connected and whether the editor is targeting the active tab's domain.
- **Language** — change the side panel's UI language. Independent of your GrowthBook web UI language. Supported today: **English**, **Spanish**, **Portuguese (Brazil)**, and **German**.
- **Dark mode** — toggles the side panel's UI theme. Independent of your GrowthBook web UI theme.
- **Change Connection** — change which GrowthBook host you're talking to (e.g., switch from production to staging, or set a self-hosted host) or change the personal access token.
- **Disconnect** — clear the stored key. Use this when handing off the browser to someone else.

[alyssa insert a screenshot of exactly this feature here: the gear menu open with Status, Language, Dark mode, Change Connection, and Disconnect labeled.]

## Next steps

- **[Running on your site](/app/visual/running-on-your-site)** — install the SDK so your variations actually render in production.
- **[AI mode](/app/visual/ai-mode)** — write your first AI-generated variation.
- **[Manual mode](/app/visual/manual-mode)** — full reference for the point-and-click editor.
- **[Visual Editor overview](/app/visual)** — back to the main page.
